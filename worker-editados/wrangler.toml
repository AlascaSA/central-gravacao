# Worker que sincroniza os editados (cron de hora em hora + endpoint pro botão "Atualizar").
name = "central-gravacao-editados"
main = "index.js"
compatibility_date = "2024-11-01"
workers_dev = true

[triggers]
crons = ["0 * * * *"]
